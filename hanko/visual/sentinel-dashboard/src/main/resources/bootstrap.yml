server:
  port: 5020
  servlet:
    encoding:
      force: true

spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: http://register:8848
  profiles:
    active: @profiles.active@
